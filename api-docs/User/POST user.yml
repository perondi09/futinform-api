info:
  name: POST user
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "name": "Perondi",
        "email": "perondi131111@email.com",
        "favoriteTeamName": "São Paulo" 
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
