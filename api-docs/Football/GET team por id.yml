info:
  name: GET team por id
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/brasileirao/teams/6685
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
