info:
  name: GET teams
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/brasileirao/teams
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
